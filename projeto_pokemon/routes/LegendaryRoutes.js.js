var express = require('express');
var router = express.Router();
const controller = require('../controller/LegendaryController');

/* GET users listing. */
router.get('/', controller/LegendaryController);

module.exports = router;
